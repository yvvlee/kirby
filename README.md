# Kirby

Kirby is a standalone configuration platform.

This repository is being extracted from its original private deployment context. The public build, local deployment, API, and contribution instructions will be added as the standalone components are implemented.

Kirby will provide a Vue 2 administration interface, an HTTP management API, and HTTP plus gRPC runtime configuration access.

See `docs/source-provenance.md` for the extraction boundary.
